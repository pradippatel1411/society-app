{
  "name": "society-app",
  "version": "0.1.0",
  "private": true,
  "description": "Society maintenance multi-tenant SaaS",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "build:web": "pnpm --filter web build",
    "build:api": "pnpm --filter api build"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}